<% provide(:title, @user.name) %>
<div class="navbar">
  <div class="navbar-inner">
    <ul class="nav">
      <li><%= link_to "Neu", new_uvp_path %></li>
      <li><a href="#">Portfolio</a></li>
      <li><a data-toggle="tooltip" title="first tooltip" href="#">Hilfe</a></li>
      <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Dropdown <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><a href="#">Opt1</a></li>
                <li><a href="#">Opt2</a></li>
                <li class="divider"></li>
                <li><a href="#">Opt3</a></li>
              </ul>
            </li>
    </ul>
  </div>
</div>

<div class="row">
  <aside class="span4">
    <section>
      <h4>
        <%= gravatar_for @user %>
        <%= @user.name %>
        <%= @user.surname %>
      </h4>
        <li> erstellt am  <%= Date.parse(@user.created_at.to_s).strftime("%d.%m.%Y") %> </li>
    </section>
  </aside>
  <div class="span8">
    <% if @user.uvps.any? %>
      <h3>Unterrichtsverlaufspläne (<%= @user.uvps.count %>)</h3>
      <ol class="microposts">
        <%= render @uvps %>
      </ol>

    <% else %>
          <h3>Keine Unterrichtsverlaufspläne</h3>
    <% end %>
  </div>
  <!-- -->
  <!-- -->
</div>